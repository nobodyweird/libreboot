menuentry 'Load MemTest86+' {
	if [ -d "(cbfsdisk)/memdisk" ]; then
		set root='cbfsdisk'
		chainloader /memtest
	elif [ -d "(memdisk)/memdisk" ]; then
		set root='memdisk'
		chainloader /memtest
	fi
}

